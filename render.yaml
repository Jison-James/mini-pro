# Blueprint Version: 1.3 - Node 20 & Explicit Build
services:
  - type: web
    name: indoor-navigation-app
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_VERSION
        value: 20
